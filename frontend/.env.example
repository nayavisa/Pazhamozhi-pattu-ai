# Frontend env vars (Vite). Copy to .env.local for local dev.
# Production values should be set in your Netlify Site settings →
# Environment variables; .env files are not deployed.

# Where the search API lives. Defaults to the production deploy if unset.
VITE_API_URL=https://pazhamozhi-api.onrender.com
